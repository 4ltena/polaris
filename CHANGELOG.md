# 変更履歴

[Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) と [Semantic Versioning](https://semver.org/lang/ja/) に従う。

## [0.12.0] — 2026-09-11 "Alrescha"

macOSの作業画面と、保存・隔離実行・ローカルモデル・会話記憶の接続を追加した。[v0.12.0 “Alrescha”](https://github.com/4ltena/polaris/releases/tag/v0.12.0)としてApple Silicon向けDMGとソースコードを配布する。追加実モデル確認など、今回省略した検証は下記に記載する。

### 追加・変更

#### macOSへの導入

- macOS 13.5以降、Apple Silicon向けに、最適化ビルドのアプリをDMGで配布する。
- DMGの上側を宇宙、下側を緩やかな弧の地球とし、PolarisとApplicationsの中央に下向きの矢印を配置した。画面内の案内文と背景用フォルダは表示しない。
- 既存SVGの6本線を使ったアプリアイコンと、宇宙・地球の背景を生成するスクリプトを追加した。
- service helper、実行helper、スキル・エージェント資源、Node.js 24.11.1、依存関係のライセンスを同梱する。
- 初回設定の6ページをアプリに含め、接続やプロジェクトの設定は後回しにもできる。
- 個人設定や認証を含めず、埋め込みモデルと追加の言語別ビルド環境は別途設定する。
- 組立てにrelease構成を指定できるようにし、デバッグ用途の既定値は維持した。
- ダウンロード表、インストール手順、SHA-256照合の案内をREADMEの日英両版へ追加した。Developer ID署名・公証はない。

#### 起動・画面配置

- macOS 13以降に対応するSwiftUIの作業画面を追加し、会話、ファイル、Git、task・childの実データを同時に表示する。
- 初回設定からプロジェクト、権限、クラウドまたはローカルの主モデルを選び、保存した構成で接続できるようにした。
- スプラッシュの初期サイズを固定し、大きな画面が一瞬表示されてから縮むちらつきと、縮小後の位置ずれを修正した。
- スプラッシュと作業画面の版番号・コードネームを同じ版情報から表示する。
- 原本SVGの透明なロゴを使用し、黒い矩形背景を除いた。ロゴは全高サイドバーの上部へ移動した。
- タイトルバーとウィンドウ上端の背景を統一し、左サイドバーを明るい配色にした。
- 作業画面のボタンを背景へなじむ表示にし、領域間の表示線を0.5ポイントへ細くした。
- 会話欄の初期幅を広げ、プロジェクト一覧、会話、ファイルツリー、作業欄の4境界をドラッグで調整できるようにした。
- 境界のキーボード操作とアクセシビリティ操作、初期配置へ戻す操作を追加した。
- 保存専用の未接続状態では接続案内を表示し、利用できないファイル・Git更新操作を無効化する。

#### 会話・入力・実行表示

- 同梱service helperから本番実行経路を起動し、保存済み認証とモデル構成で実モデルへ送信できるようにした。
- モデル・推論強度・権限の変更を保存し、以前の実行所有者の終了後に新しい設定へ接続し直す。
- モデルを変更しても以前の構成へ戻り、送信できなくなる接続不具合を修正した。
- Enterで送信、Shift+Enterで改行できる共通入力欄を接続した。日本語の変換確定Enterを送信として扱わない。
- 送信した入力、応答本文、実行状態を保存済み履歴と統合し、送信内容が会話へ反映されない問題を修正した。
- 新しい入力・応答が履歴の途中へ入る問題を修正し、長い会話の末尾まで読み込む。520件の履歴と更新後の末尾保持を回帰検証した。
- ファイル更新時に、選択中のタブと縦横のスクロール位置を保持する。
- task・childと実行状態を会話へ投影し、未報告のモデル名や試行回数を推測表示しない。
- 承認待ち通知を折りたたみ欄の外へ表示し、通知から対象欄へ移動できるようにした。
- 取消の受付、終了待ち、結果保存を分け、受付だけで実行完了と表示しない。
- UTF-8テキスト添付の選択、プレビュー、確認後の保存・送信を追加した。最大4件、各64KiB、合計128KiBとし、同じファイルの重複取込みを拒否する。
- 添付本文を下書きへ一度だけ展開し、終了・再起動後にも復元する。
- macOSの音声入力を下書きへ接続し、認識結果を送信前に確認できるようにした。

#### ファイル・隔離実行・変更反映

- プロジェクトのファイル本文とGit状態、選択ファイルの最新履歴を保護された読取経路へ接続した。
- 秘密名、登録済み秘密ファイル、link、生成物、バイナリ、プロジェクト外のパスをファイル読取・添付の対象から除く。
- Git読取の引数と設定を固定し、external diff、textconv、hook、fsmonitorの呼出しを抑止する。
- 実行ごとに隔離した作業領域を用意し、モデルによる編集を元プロジェクトへの反映と分離する。
- 子プロセスの環境とファイル記述子の継承を制限し、許可された実行範囲と取消処理を接続した。
- 元ファイルへの変更候補を一覧化し、対象パスと変更前後のハッシュを全件確認した後に反映を許可できるようにした。
- 反映の許可受付と結果保存を分け、書込み・削除・復旧の件数と結果を画面へ自動反映する。
- 承認待ち中の外部編集を競合として扱い、その内容を上書きしない。
- 期限切れ・取消済みの反映承認を無効化し、失効時は元ファイルを変更しない。
- 保存済みの反映回答を読み直しても再反映せず、結果不明時は状態の照合を求める。

#### ローカルモデル・役割設定

- OllamaとLM Studioの導入済みモデル一覧を、明示的な更新操作で取得できるようにした。
- 接続先への到達性と、ロード済み・未ロード・不明の状態を表示する。到達不能を未導入とは判定しない。
- ローカルの主モデルを選択して会話へ使用し、子エージェントの役割ごとにもモデルを割り当てられるようにした。
- ツール必須役割では、ツール対応を確認できないモデルの割当を拒否し、実行前にも構成を照合する。
- 役割設定の連続保存と保存後の再接続を修正し、設定競合では現在の保存内容を明示的に採用できるようにした。
- 設定保存の結果が不明な場合は自動再送せず、保存済み構成を照合する。未設定役割をクラウドへ自動転送しない。

#### 会話記憶・使用量

- GUIの履歴設定から従来方式または`strict10`を選択できるようにした。旧設定との互換性を保ち、主モデル変更後も選択した履歴方式を保持する。
- デスクトップのv3保存先で全原文を保持し、`strict10`の直接投入を直近10実ユーザーターンに限定する。それ以前は要約とローカル埋め込みから検索する。
- 要約の取得を同じproject・session・epochに限定し、出典のhashと削除状態を再照合する。
- `conversation://`から必要な原文ページを取得できるようにした。表示履歴を削らず、取得した資料を新しい原文履歴として重複保存しない。
- 出典情報の重複によって記憶が検索から除外される不具合を修正した。要約1件256・検索合計768参照トークンを維持する。
- 要約が参照予算を超える場合は黙って除外せず、主要求の前に理由を表示して停止する。
- `strict10`の固定埋め込み資源を起動時に照合し、不足・不一致の理由をGUIへ表示する。モデル資源を自動取得しない。
- 主回答、要約、ローカル埋め込みの使用量を別々に保存・表示し、欠測をゼロ消費とは扱わない。
- 取消後に次の記憶処理を開始せず、結果不明の要約を自動再送しない。

#### 保存・復旧・起動資源

- 会話、下書き、実行、承認、workflowと子エージェントの状態を、プロジェクト外の保存先で単一writerが管理する。
- 同じ保存先の二重所有を拒否し、先行アプリの終了後に会話と下書きを復元できるようにした。
- 登録したプロジェクトフォルダが別の実体へ差し替えられた場合、保存済み会話を保持して再接続を拒否する。
- helper喪失後は結果不明として復旧し、同じモデル要求を自動で再送しない。
- ウィンドウ終了時に下書き保存と所有helperの終了を待ち、再接続・終了処理の重複を防ぐ。
- native起動の認証保護先をOSが返すホームへ固定し、接続診断には資格情報や応答本文を含めない。
- 同梱実行helperのハッシュとカタログ・Schemaを起動時に照合し、必要な資源が不足した構成で実行を開始しない。
- 組立て時に明示された移設可能toolchainを同梱し、入力のlink・特殊ファイルを拒否する。
- Git管理外の`.polaris/skills`に依存した梱包不具合を修正し、同梱スキル2件を`skills/`で管理する。個人設定なしのソースでも組み立てられる。
- 同名のローカルスキルがあってもアプリへ混入させず、Git管理された資源だけを同梱する。
- dev版のsha2を最適化し、全量SHA検査による起動遅延を改善した。照合内容と起動期限は維持する。
- Cargo workspaceとmacOSアプリの版表示を0.12.0 “Alrescha”に揃えた。

### 検証・制約

- 配布準備の梱包試験7件、最適化ビルド、DMGの整合性、アプリ534ファイルの照合、移設後の所有者・権限、Finderでの上下配置を確認した。配布用アプリから追加の実モデル送信は行っていない。
- Rust全体1,689件、Swift全体236件、梱包試験6件が成功。Clippy全workspace/all-target（warnings deny）、fmt、filemapも成功した。個人設定と既存Swiftビルドキャッシュなしのアプリ組立てを確認した。添付・役割再接続・終了保存・新着会話表示はGUIで確認した。
- 音声入力は合成試験のみ。Windows・Linux、署名配布、実録音、実電源断を検証済みとはしない。
- strict10の実GUI試験12入力中、11番目の原文参照は成功し、12番目で検索不具合を検出した。修正後は自動回帰とアプリの接続・履歴復元が成功。追加実送信は省略したため、実モデル受入の合格とは扱わない。
- Codex CLI 0.154.0との比較はPRE01を除く15組の部分結果。追加測定を省略したため、全16組の結果とはしない。
- 過去版の比較結果を本版の性能として扱わない。

## [0.11.0] — 2026-09-08 "Sadalmelik"

作業段階を保持するworkflowを標準採用し、共通・段階別skillの読込みと会話の再開・分岐を整えた。企画・仕様・計画の16組では、workflow有効のPolarisは純正Codex比で総tokensを79.8%、固定API単価の換算費用を40.8%削減し、両者の品質検査が合格した。

### 追加

- 共通ルールと段階別skillを分類器とは別に解決し、本文・出所・hashをターン単位で固定するworkflow。新規会話では既定で有効となり、`--phase`で開始段階を指定できる。
- workflow状態と原文を保存するsessions-v2、再開・分岐、世代照合と削除marker。旧会話の取込み時は元のJSONLを保持する。
- 実験的な`strict10`履歴モード、型付き要約、固定ローカル埋め込み、原文ページの再取得。必要なruntimeや設定がない場合は開始前に停止する。
- hosted Webの要求構築と引用表示、通常応答・要約・子実行・埋め込みを区別する使用量台帳API。
- 企画から計画レビューまでの8ケース、2ターンの条件変更、客観検査と内容評価による実モデル比較。

### 変更・修正

- workflowは必須機能として採用し、費用削減だけを採用条件としない。比較用の明示的な無効設定は維持する。
- 保存ロックを明示的に解除し、別プロセスへ複製された記述子が残る場合の再開失敗を修正。CLIの分岐でも保存済みphaseを復元する。
- `[files_md] auto_regenerate=false`で自動更新要求を止められるようにした。既定と手動実行は維持する。
- 日英READMEをworkflow有効版と純正Codexの比較、利用開始、主な機能、詳細文書への案内へ整理した。
- 全クレートの版番号を0.11.0に統一する。

### 検証・制約

- 既定変更後の全workspace試験1,053件、全target Clippy、fmt、filemapが成功した。
- workflowと純正Codexの比較はgpt-6-astra／medium、同じ16試行・各2ターン。各32回答の客観検査と各16試行の内容評価が合格した。使用量をCLIと保存セッションで照合した。
- 比較は別時刻に行った短い合成課題で、実装能力や長期会話の品質を保証しない。換算費用は実請求や利用枠の測定ではない。
- strict10と要求間隔制御は任意設定。strict10・Webの実モデル評価は未完了で、Webは現行CLIで有効化できない。通常CLIの台帳はメモリ内で、永続台帳APIとは区別する。
- AstraのAPIキー用Responses対応は未実装であり、ChatGPT認証の`codex`経路とは区別する。

測定条件と全結果は[実装前段階の評価](docs/preimplementation-evaluation.md)、回帰試験と未検証範囲は[v0.11.0の検証状況](docs/sadalmelik-validation.md)を参照。

## [0.10.0] — 2026-09-07 "Algedi"

保存結果の再取得と隔離実行を整え、本文量を維持したキャッシュ改善を検証した。5秒間隔の要求制御は任意設定として残し、既定offを維持する。長期対話の1組では総トークン同一でAPI換算費用が15.38%減ったが、別課題の対照側が品質不合格となり、全体比較は未完了である。

### 追加

- `memory://ID/search/QUERY`で保存した1記録だけを検索する。プロジェクト・削除済み記録の境界、原文の範囲取得と出力上限を維持する。
- Codex向けの要求間隔制御`POLARIS_CACHE_PACING=on`。同じproviderのモデルPOSTを最低5秒間隔にし、待機中のキャンセルと認証再試行に対応する。既定off。
- キャッシュ接頭辞と同一ターン内の通信継続に関する実験設定、要求ごとの待機時間・開始間隔・使用量診断。
- 36ターンの履歴維持、zero/one/few-shot、skill 0・3・30個、tool 0・3・6個を扱う品質検証と、モデル・usage・変更範囲を確認する実測スクリプト。
- Polarisのバナー。READMEでは背景の明暗に応じて濃色版と白色版を表示する。

### 変更・修正

- 両providerの既定モデルを`gpt-6-astra`、推論強度を`medium`に統一。明示したモデル・推論強度は優先する。
- 既存のfilemapがある場合は参照先を案内し、固定コンテキスト990トークン以下を維持する。
- 認証ストアの読取専用モード、証明書bundleを使うHTTP設定、隔離実測用のツールbrokerを追加。境界や認証更新を許可できない場合は停止する。
- READMEを要点中心に整理し、利用方法・テスト・比較の詳細をdocs直下へ分離する。
- 全クレートの版番号を0.10.0へ統一する。

### 検証・制約

- 版確定前の回帰検証は全workspace 964件、実測driver 6件、Python 46件が成功。fmt・全target clippy・filemap検査も成功した。
- 要求間隔の比較は4試行中3試行完了。125ターン中124合格、149モデル要求のusageを照合した。品質停止後の追加試行と完了済み試行の再実行は行っていない。
- キャッシュ用の固定文追加や通信継続設定は採用条件を満たさず、既定変更を見送った。要求間隔制御も既定off。料金はAPI単価を当てた参考額であり、Codexの実請求や利用枠ではない。
- `--tool-memory`も既定offを維持する。自動ベクトル化、web search、段階別の常時skillは今回追加していない。

詳しくは[要求間隔の結果](docs/gpt6-cache-pacing-results.md)、[通信継続と品質検証](docs/gpt6-cache-affinity-results.md)、[キャッシュ率と費用](docs/gpt6-cache-cost-results.md)を参照。

## [0.9.0] — 2026-09-06 "Rukbat"

大きなツール結果を保存し、必要な根拠だけを再取得する機能を追加した。GPT-6 mediumの同一合成課題では、現在のユーザー設定を含むCodex CLIとの比較で総トークンを93.7%削減した。一般の実装作業へ適用できる削減率ではない。詳細は[実測結果](docs/gpt6-efficiency-results.md)を参照する。

### 追加

- `--tool-memory history|retrieval`でread・bashの大きな結果をローカルへ保存し、短い参照に置換する。既定はoff。保存に失敗した場合は全文を保持する。
- 既存readから`memory://`経由で原文、範囲、検索抜粋を取得する。行・段落を優先した複数抜粋を返し、重複する範囲を統合する。
- 任意のローカル埋め込みAPIによる意味検索を設定できる。未設定時はキーワード検索のみ。埋め込みの使用量と欠測を区別して記録する。
- Codexプロバイダの要求別計測、入力構成のハッシュ、モデル・使用量・正答・変更範囲を確認する比較スクリプト。
- `memory get`の範囲取得と、検索時の根拠抜粋。

### 修正・変更

- 履歴圧縮の再試行を制御し、圧縮後も必要なツール往復の対応を保つ。
- files.mdの更新をまとめ、重複した更新を減らす。
- TUIの分岐先は自身の記憶へ保存し、検証済み祖先を読み取り専用で参照する。削除済み祖先への依存を検査する。
- 全クレートの版番号を0.9.0に統一する。

### 検証・制約

- ツール記憶・TUI・計測スクリプトの対象テスト、整形、静的検査は成功。全体テストは実行環境のポートbind制限で認証系11件が失敗し、全件成功とは扱わない。
- 実際の埋め込みAPIとの接続は未検証。意味検索の実証済みリリースとは扱わない。
- 保存する原文は既存ツールの出力上限適用後の本文。検索抜粋はすべての一致箇所や訂正を網羅するものではない。
- 一律8KiB読取制限は実測で悪化し、明示キャッシュ設定はCodex接続先が拒否した。実験用設定として残すが、既定採用しない。

## [0.8.0] — 2026-09-05 "Antares"

使用量の集計範囲を揃え、圧縮前の履歴をプロジェクト別に保存・検索する機能を追加した。トークン削減率は未測定である。

### 追加

- `--remember`で圧縮前の履歴保存を有効化する。保存に失敗した場合は会話履歴を維持する。
- プロジェクト別のSQLite記憶ストアと`memory search/get/import/index/embed/forget`。部分一致検索と、明示的に設定したローカル埋め込みサーバーを使う意味検索に対応する。
- `--compact-at`で自動圧縮の閾値を指定できる。既定値200,000トークンは維持する。
- `--version`による製品版の表示。

### 修正

- 使用量に圧縮の要約要求と子エージェントの要求を含める。応答の欠測、失敗、中断はゼロ消費と区別して表示する。
- 再開用ログの置き換えを一時ファイルからの原子的な更新に変更する。圧縮前原文は再開用ログと分離する。
- 記憶の保存と削除をプロセス間ロックで排他し、削除済みセッションの再取り込みを防ぐ。別プロジェクトから再開・分岐した会話の出自を保持する。

### 変更

- 全クレートの版番号を`workspace.package.version`から継承し、製品版の0.8.0に統一する。

### 検証・制約

- 記憶ストア、CLI、TUI、使用量計測、圧縮の対象テストは成功した。全体テストは実行環境のソケットbind制限で停止しており、全件成功とは扱わない。
- 実モデルの埋め込み接続、実対話での消費量比較、対話端末の操作確認は未実施。
- 過去の記憶は自動的に会話へ挿入しない。`forget`は管理下の索引・原文を削除するが、元の再開用ログや取り込み元ファイルは削除しない。

## [0.7.0] — 2026-08-27 "Zubenelgenubi"

プロンプトキャッシュ周りに3つの変更を加えた。当初、polarisと純正codexクライアントで同種のタスクを比較し、polarisは8リクエスト中キャッシュ0件、codexは63.4%がキャッシュヒットしているという差を観測していた。この差を埋める狙いで`prompt_cache_key`の送信とCodexプロバイダの`include`常時送信を追加したが、後日、修正前後のビルドを直接比較したところ、修正前のビルドでも同一プレフィックスが繰り返される3ターン目以降にはキャッシュ命中率が93〜97%に達しており、この2点が原因だったという説明を再現できなかった。実際には`agent::run_loop`側の集計漏れが当初の観測を作っていた可能性が高い(下の修正2件目)。`prompt_cache_key`・`include`の2点は害が無く理屈の上でも妥当なため変更自体は残すが、効果は未検証として扱う。

機能を2つ足した。Codexプロバイダのreasoning item継続と、自動履歴圧縮である。前者はResponses APIが返す暗号化済みのreasoning itemを会話履歴に保持し、次のターンでそのまま送り返す。後者は会話が閾値を超えた時点で、古いターンを1件の要約メッセージへ置き換える。閾値は開発中に100,000から200,000へ引き上げたが、その判断の根拠は実測していない。同種のキャッシュ効率の主張が上記の通り再現しなかった経緯があるため、コード側のコメントにも未検証である旨を残した。

### 追加

- `prompt_cache_key`の送信。モデル・reasoning effort・システムプロンプト・ツール定義のFNV-1aハッシュを鍵とする`cache_key`関数を追加し、Codex・OpenAI両プロバイダのリクエストに含めるようにした。会話内容そのものは鍵から除外しているため、ターンが進んでも鍵が変わらない。ロードバランスされたバックエンドでのルーティング一貫性を狙った変更で、効果自体は上記の通り未検証
- one-shot実行(TUIを使わない`exec`系サブコマンド)でも、TUIの`/status`と同じ`tokens: in N / out N / cache N / total N — Mメッセージ`の内訳をstderrへ出力するようにした
- Codexのreasoning item継続。`ReasoningItem`型を追加し、SSEストリームから捕捉して`Session`へ通し、次ターンのリクエストへ再送する。上流codexの挙動に合わせ、再送時は`id`を落とす。OpenAI Chat Completions側にはこの概念が無く、渡しても無視されることをテストで確認した
- 自動履歴圧縮。`session.messages`の実測トークン数が`COMPACTION_THRESHOLD`を超えると、直近2件のユーザーターンを残し、それより前を1回のLLM呼び出しで生成した1件の要約メッセージへ置き換える。切る位置は必ず`Role::User`に限り、ツール呼び出しと結果の対を割らない。要約が失敗するか空で返った場合は履歴を変更せず、そのターンをそのまま続ける
- TUIの`/compact`スラッシュコマンドと、圧縮が起きたことを示す通知行
- system promptへ検証ルールを2条追加した。数量・因果関係の主張は実出力で裏付けること、path:lineの引用は実際にその主張を裏付けていることを求める。予算超過を避けるため`CONSTITUTION_LIMIT`を150から100トークンへ引き下げた。この過程で、以前コメントに残っていた「626トークン/約360トークンの余裕」という記述が実測ではすでに余裕ゼロまで劣化していたことが判明し、コメントを実測に基づいて書き直した

### 変更

- `COMPACTION_THRESHOLD`を100,000トークンから200,000トークンへ引き上げた。圧縮のたびに要約が新しく生成され、プロンプトキャッシュが再利用していた接頭辞を捨てることになるため、頻繁な発火はキャッシュを温まらせない。ただしこの効果は未実測である
- `read`ツールの説明文を2段階で書き直した。`limit`省略時の既定値(2,000行)を明示し、続けて小さい`limit`を推測するより省略を既定選択とするよう表現を強めた。同一プロンプト(「このプロジェクトを分析する」を`polaris`自身のリポジトリへ)でのA/B実測で、往復数16→8→7、raw total 699,872→521,304→461,611(-34%)、キャッシュヒット率88.1%→78.5%→73.5%という結果になった。往復を減らすほどキャッシュ再利用の機会も減るという表裏の関係で、`store: false`のままではこの2つを同時に改善する余地は小さい

### 修正

- Codexプロバイダが送る`include: ["reasoning.encrypted_content"]`を、環境変数によるオプトインから常時送信へ変更した。reasoning対応モデルのプロンプトキャッシュに関わるという実測付きの内部コメントが根拠だったが、修正前後の直接比較ではこの設定の有無による明確な差を確認できていない
- `agent::run_loop`がターン内の応答を積算する際、`cached_tokens`だけ加算し忘れていた不具合。`/status`のキャッシュ内訳表示が、実際のキャッシュ利用状況によらず常に`cache 0`のままになっていた
- 圧縮がターンの途中で走ったとき、チェックポイントが古いまま残る不具合
- `/compact`をタイプして実行した経路が、スラッシュコマンド経路と挙動を揃えていなかった問題

### 備考

- 閾値を下回るコンテキストウィンドウのモデルでは、圧縮が発火する前にウィンドウ上限へ達するため、機能そのものが働かない。gpt-5.x系を対象とする限り表面化しないが、小さいウィンドウのプロバイダを足す際に最初に見直す箇所になる
- 圧縮が内部で投げる要約リクエストの消費量は、どの集計にも入っていない。`compact`は応答の本文だけを読んで`usage`を捨てるため、`tokens:`行にも`/status`にも現れない
- polarisと純正codexで同一タスクのraw totalを比較したところ、根本原因は`store: false`(毎ターン履歴全体を再送)対upstreamの`previous_response_id`(前回からの差分のみ送信)という設計の違いであり、2つの「total tokens」はそもそも同じものを計測していないと判明した。両立を構造的に実現するには`store: true`化が要るが、会話をサーバー側に保持することになり現行の設計原則からの転換となるため、着手は別途brainstormingから
- 調査の過程で、audit log(`~/.polaris/state/<project-id>/audit.jsonl`)の`result`/`detail`フィールドがログ保存用に4 KiBへ切り詰められる(`MAX_RESULT_BYTES`)ことを確認した。この値は会話コストの代理指標にならない

## [0.6.0] — 2026-08-25 "Spica"

対話TUIの描画方式を、`ratatui::Viewport::Inline`(端末のネイティブスクロールバックへの一度きり印字)から、`Viewport::Fullscreen`による自前のスクロール管理へ移行した。Terminal.app を含む主要ターミナルが「新規出力・キー入力で強制的に最下部へスクロールする」挙動を無効化できないことを tmux での実地検証で確認したため。加えて、入力欄の編集機能を拡張した。

### 追加

- 入力欄でのカーソル移動: 左右キーでカーソルを移動し、その位置に挿入・削除できるようにした(従来は常に末尾への追記・末尾からの削除のみ)
- 入力履歴の呼び出し: Up/Downキー(一般的なターミナル/シェルと同じ)で、今回のTUIセッション内で送信した過去の入力を辿れるようにした(セッションを跨いだ永続化はしない)
- 会話履歴の自前スクロール: `history: Vec<HistoryLine>` にヘッダー・会話・ツール進捗を蓄積し、`scroll_offset` に応じた窓だけを毎フレーム描画する方式へ変更。PageUp/PageDownキーで履歴をスクロールできる(候補ポップアップ表示中はUp/Downが候補選択に使われる)
- フッター(model/effort/入力欄)を画面最下部に常時固定表示
- 簡易Markdown整形に見出し(`#`〜`######`)・箇条書き(`-`/`*`)を追加。見出しはマーカーを除去して行全体を太字で描画し(内側の`**太字**`/`` `インラインコード` ``も引き続き整形される)、箇条書きはマーカーを`•`に置き換える(ネストのインデントは保持)。フェンス付きコードブロック内の`#`・`-`は対象外
- `read`等のツール結果プレビュー行、応答文中のフェンス付きコードブロック、インラインコード(`` `code` ``)を、専用の文字色(`#989DCC`)で表示し、通常の文言と視覚的に区別できるようにした(全言語対応のシンタックスハイライトはスコープ外)
- 会話履歴の行が表示幅を超える場合、単語境界で複数行へ折り返すようにした(スタイル・網掛けを保ったまま)。従来は端末幅を超える行が右端で切り詰められていた
- `/status`にキャッシュトークンの内訳を追加(`cache N`)。OpenAI Chat Completions APIの`usage.prompt_tokens_details.cached_tokens`、Responses API/Codexの`usage.input_tokens_details.cached_tokens`をそれぞれ読む
- 入力欄の網掛けを、上下に網掛け済みの空行を1行ずつ追加して太くした
- ChatGPT Plusサブスクリプションでは、既定のモデル/reasoning effortを`gpt-5.6-sol`/低(low)から`gpt-5.6-terra`/高(high)へ変更した(`--model`/`POLARIS_MODEL`を明示指定した場合はそちらが優先される)
- ツール呼び出しのライブプレビュー(`⏺ read(...)`)を、生のJSON(`{"path":"a.txt","limit":50}`)ではなく`path="a.txt", limit=50`形式の呼び出し風表記で表示するようにした
- `/copy`コマンド・`Ctrl+O`ホットキーを追加。直近のpolarisの返信をOSC 52エスケープシーケンス経由でクリップボードへコピーする(tmux・SSH経由でも動作、実コードは純正codexの`clipboard_copy.rs`/`copy_last_agent_markdown`を参考にした)
- マウスによるドラッグ選択・コピーを実装した。会話履歴上をドラッグすると選択範囲が青系のハイライトで表示され、ボタンを離すと選択したテキストが自動的にOSC 52でクリップボードへコピーされる。直近の返信をコピーする`/copy`・`Ctrl+O`とは別の経路として共存する。全角文字を含む行でも正しい範囲を選択でき、ドラッグ中に画面の上端・下端へ近づくと自動スクロールする。別の場所をクリックする、文字を入力する、`/new`・`/resume`・`/fork`・`/clear`を実行する、端末をリサイズするといった操作で選択は解除され、アイドル時・ターン実行中のどちらでも同様に動作する。あわせてマウスホイールでの会話履歴スクロールも復元した。端末のマウスキャプチャを常時有効化すると端末ネイティブのドラッグ選択が一切効かなくなり、xtermのマウスプロトコルの制約上ホイールスクロールと端末ネイティブ選択は両立できないため、polaris自身が選択機能を持つことでこの制約を回避した

### 変更

- `/model`・`/skills`・`/permissions`・`/resume`の各ピッカーは、専用の一時的なフルスクリーン切替(`with_fullscreen_picker`)を廃止し、常時フルスクリーンの共有ターミナルへ直接描画するよう簡略化した
- 履歴をスクロールしている最中にライブなツール進捗イベントが追加されても表示位置が動かないよう、スクロール中は追加行数だけオフセットを補正するようにした
- ターン実行中もPageUp/PageDownキーで履歴をスクロールできるようにした(従来はアイドル時のみ)
- 新しいメッセージを送信すると自動的に最下部(最新行)へ戻る
- `/resume`・`/new`・`/fork`・`/clear`のいずれでも、ヘッダーボックスの再描画とスクロール位置のリセットを確実に行うようにした

### 修正

- IME確定前文字(未確定文字)のポップアップが、実際のカーソル位置ではなく画面下端に表示されていた不具合。ターミナルの実カーソル位置を毎フレーム報告するようにした
- `Viewport::Fullscreen`への移行に伴い、alternate screen(代替スクリーンバッファ)への出入りを明示的に行うようにした。従来のインラインモードはこれに依存しない設計だったため未対応のままだと、フルスクリーン化後にユーザーの実ターミナル画面を上書きし続け、終了時に元へ戻せない不具合になるところだった
- `/model`等のピッカーを一度でも開閉すると、共有ターミナルが恒久的に縮小表示(旧インラインモード相当)へ戻ってしまい、以後の会話履歴が一切表示されなくなる不具合(`with_fullscreen_picker`廃止と同時に解消)
- スラッシュコマンドの候補一覧で、表示件数(8件)を超えて選択位置を下へ移動すると、内部では選択が移動しているのに表示窓が追従せず、選択中の項目が画面外のまま見えなくなる不具合。表示窓が選択位置を追うようにした
- 入力欄が空のとき、点滅カーソルが実際にはプレースホルダ文字列("Ask polaris to do anything")の先頭の"A"の上に重なって表示され、点滅する大文字の"A"に見えてしまう不具合。カーソル形状を(端末既定のブロックではなく)細い縦棒に変更し、下の文字を覆い隠さないようにした
- ChatGPT Plusサブスクリプションで、実際にAPIへ送信されるreasoning effortは正しく`high`だったにもかかわらず、フッターの表示だけが常に既定値の`low`のままになっていた不具合。起動時に保存済みアカウントのプランからeffortを判定し、フッターの初期表示に反映するようにした
- 入力欄を含むフッターが画面の最下段に届かず、下に空白が残っていた不具合。候補ポップアップの最大表示件数を想定した固定高さを常に確保していたことが原因で、ポップアップが出ていない通常時は余った分が下に空白として残っていた。実際に必要な高さを毎フレーム動的に計算するよう修正し、会話履歴とフッターの間に隙間ができないようにした

## [0.5.0] — 2026-08-24 "Regulus"

M4(subagent オーケストレーション基盤)を実装し、これを土台に2つの機能を追加した。会話コンテキストを一切消費せずディレクトリ構成の要約を保守する `files.md` 自動生成と、ツール呼び出し・subagent進捗をターン実行中に逐次表示するライブ表示改良。加えて、TUIの端末統合方式を `codex` の実挙動に合わせてインラインビューポートへ切り替えた。

### 追加

- `spawn` ツール: 1回の呼び出しで複数の subagent を同一波として並列実行する単一波オーケストレーション。書込先(`write_root`)が波内で重複する場合は1件も起動せず波全体を拒否
- `agents/<type>/SKILL.md` による subagent 型定義の frontmatter 解析・discovery(`polaris-access`/`polaris-tier`/`polaris-wall-seconds`/`polaris-max-turns`/`polaris-continuation`/`polaris-output` の各メタデータ)。実例として `file-inspector`(読み取り専用)・`files-md-writer`(読み書き)の2型を同梱
- subagent の結果は宣言済み JSON Schema で検証し、不一致時は検証エラーを添えて1回だけ再試行。壁時計超過・スキーマ不一致2回連続をそれぞれ専用の停止条件として追加
- ディレクトリ別 `files.md` 自動生成: `write`/`edit`/`bash` 実行前後のファイルシステムスナップショット差分から新規ディレクトリ・新規ファイルを検出し、`files-md-writer` subagent を直接起動して該当ディレクトリの `files.md` を最新化する。モデルの判断・会話コンテキストを一切介さず、ハーネス側で決定的に実行
- ツール呼び出し・spawn進捗のライブ表示: `polaris_core::agent::run`/`dispatch` から `AgentEvent`(`ToolStarted`/`ToolFinished`/`SpawnStarted`/`SpawnFinished`)を通知する経路を新設し、TUIがターン実行中にツール名・引数・結果プレビュー、write/editの実差分(40行で打ち切り)、spawnタスクごとの進捗を逐次表示する
- 監査ログに `caller` フィールドを追加し、ルート("root")と各 subagent(型名)の呼び出しを区別して記録

### 変更

- TUIの端末統合方式を、独自のフルスクリーン管理から `ratatui::Viewport::Inline` へ変更し、会話履歴を端末自身のネイティブスクロールバックに一度きり印字する方式に変更(`codex` の実端末挙動を tmux で検証したうえで移植。この方式は v0.6.0 で自前スクロール管理の `Viewport::Fullscreen` へ再度置き換えられた)
- `dispatch` を `async fn` 化(spawn の並列実行を await できるようにする土台)
- `AuditLog`/`Provider` を `Arc` 化し、複数 subagent から共有できるようにした

## [0.4.0] — 2026-08-24 "Acubens"

対話TUIを大幅に拡張した。TUI v2(ステータスバー・ツール呼び出しの可視化・Markdown整形)、初回起動時のオンボーディング画面、codex互換サブコマンド、対話中のスラッシュコマンド群、非同期イベントループ化によるライブなステータス表示を追加した。`codex`の実際のソースコード(GitHubから取得)とデスクトップのスクリーンショットを繰り返し参照し、対応する画面要素の見た目・操作感を検証したうえで移植した。

### 追加

- TUI v2: 画面下部のステータスバー(プロバイダ・モデル・累積トークン使用量)、ツール呼び出しの会話履歴内可視化(`⚙ ツール名(引数)` / `→ 結果`)、`**太字**`・`` `インラインコード` ``・フェンス付きコードブロックの簡易Markdown整形
- オンボーディング画面: プロバイダの資格情報が一切無い状態で対話TUIを起動すると、ChatGPTサインインまたはAPIキー入力を選べる画面を表示する(`POLARIS_PROVIDER`未設定時のみ)。APIキーは`~/.polaris/api_key.json`に0600で保存
- codex互換サブコマンド: `exec`・`sandbox`・`doctor`・`completion`(bash/zsh/fish/powershell/elvish)
- TUIスラッシュコマンド(全16個): `/help`・`/status`・`/skills`・`/new`・`/resume`・`/clear`・`/init`・`/model`・`/diff`・`/review`・`/permissions`・`/fork`・`/export`・`/pwd`・`/logout`・`/quit`
- `/model`: モデル選択→reasoning effort選択の2段階ピッカー(low/medium/high/extra high/max/ultra)。`polaris_provider::Provider`に`set_model`/`set_effort`(内部可変性で実現)を追加し、セッション中に実際に送信モデル・reasoning effortを切り替えられるようにした
- `/resume`: 保存済み会話を起動元ディレクトリ別にグループ化して選べるフルスクリーンピッカー(今いるディレクトリのグループが常に先頭)。会話の永続化先を「プロジェクトごとのディレクトリ」から全プロジェクト共有の`~/.polaris/sessions/`へ移行し、1会話=1ファイルペア(`<id>.jsonl` + `<id>.meta.json`)へ再設計
- `/permissions`・`/skills`: 対話的ピッカー(承認ポリシーの変更、発見済みskillのフルスクリーン一覧)
- ターン実行中のステータス行にshimmerアニメーション付き経過秒表示+esc中断機能。イベントループを非同期化(`tokio::select!`)して実現
- ヘッダーボックス(dimスタイルの枠線+`✦ polaris`見出し+`model:`/`directory:`/`tokens:`各行)、2色構成のフッター(モデル名・reasoning effort・作業ディレクトリの`~`短縮表示)

### 変更

- セッション永続化モデルを「プロジェクトごとに1つの会話を自動再開」から「常に空の状態で起動し`/resume`で選ぶ」方式へ変更
- `/new`の意味を「その場で消去して再開」から「新しい会話へ切り替え、元の会話は保存したまま残す」へ変更(元の会話は引き続き`/resume`から辿れる)
- プロバイダ解決: `POLARIS_PROVIDER`未指定時、保存済みcodex資格情報の有無を見て既定プロバイダを決めるようにした

### 修正

- プロバイダ未指定時、保存済みcodex資格情報があってもデフォルトが常に`openai`に解決されていた不具合
- `/model`のreasoning effortピッカーで表示名(`"extra high"`等、空白入り)をそのままAPIへ送信し実際に`400 Invalid value`を引き起こしていた不具合。APIへ送る値と画面表示名を分離して修正

## [0.3.0] — 2026-08-21 "Castor"

対話TUIを追加した。`polaris` を `--prompt` なしで実行すると、スクロール可能な会話履歴と入力欄を持つ対話セッションに入る。一発実行(`polaris -p "..."`)の挙動は変わらない。

### 追加

- 新規クレート `polaris-tui`。`ratatui` + `crossterm` によるTUI描画・入力処理・承認モーダルを持つ
- セッションの永続化。`~/.polaris/state/<project-id>/tui-session.jsonl` に1メッセージ1行で保存し、次回起動時に再開する。破損行は切り捨てて警告し、それより前の履歴で再開する
- ツール実行承認をTUI内でインタラクティブに行う機能(`y`/`n` のモーダル)

### 変更

- `polaris_core::project` に `project_id` / `state_dir` を公開関数として移動(旧 `polaris-cli` の非公開関数から)

### 修正

- 会話履歴のレンダリングにおけるスクロールオフセットの計算誤り(枠線分の行が考慮されておらず最新メッセージが見切れる不具合)
- 複数行のアシスタント応答が1行に折り畳まれて表示される不具合
- ターン失敗時にツール呼び出しとツール結果の対応が崩れたままセッションが継続し、以降の全ターンが失敗し続ける不具合
- `ratatui::init()` 以降に出力していた診断メッセージ(破損セッション警告・永続化失敗)が画面に一切表示されない不具合
- セッションファイルへの不正UTF-8バイト列でTUIが起動不能になる不具合
- キーリリース/リピートイベントを未フィルタのまま処理し、文字が二重入力されうる不具合

### セキュリティ

- LLM応答・ツール呼び出し理由・エラーメッセージなど外部由来のテキストをそのまま端末へレンダリングしていたことによる、端末エスケープシーケンス注入の可能性を修正。制御文字(ESC・DEL等)をレンダリング前に無害化する

### 備考

- 会話履歴のスクロールバック(過去ログを遡る操作)は未実装。既知の制限として `README.md` に記載した
- 実端末を要する対話的な動作確認(入力・承認モーダル・再開)は自動化していない。手動確認手順を `README.md` に記載した

## [0.2.0] — 2026-08-20 “Aldebaran”

skill/plugin 分類機(M3b)を追加した。BM25ランキングによる skill 検索と、常時コンテキストを skill 数に依存させない near-universal 選定を実装した。

### 追加

- BM25ランキングによる `skill` ツールの検索機能
- near-universal skill の選定(常時コンテキストへの混入を防ぎつつ `lookup` の戻り値にのみ載せる設計)
- 候補一覧の description をプレビュー表示(先頭200バイト)に短縮し、上位8件のみプレビューを残す方式
- codex・pi・polaris 三者の実測比較(830件規模のskill/pluginコーパスでの初回ターン入力トークン数、実行速度)

### 修正

- CJK/部分文字列検索の recall 低下、near-universal がバイト上限で切り詰められる不具合
- `read` ツールの出力バイト上限が `limit` 指定と独立していなかった不具合

## [0.1.0] — 2026-08-20

ヘッドレスの一発実行エージェントとして出荷した。M1(核となるループ)からM3a(skillsローダ)までを含む。

### 追加

- パスポリシー付き `read` ツール、OpenAI互換プロバイダ、エージェントループ、CLI(M1)
- `write` / `edit` / `bash` とサンドボックス、承認境界(M2)
- Codexプロバイダ。`polaris login` によるChatGPTサブスクリプション認証(APIキー不要)(M2.5)
- SKILL.md frontmatterの解析・検証とskillsローダ、`skill` ツール(M3a)
- 伏字化付き追記専用監査ログ、量的な停止条件
- 常時コンテキストを990トークン以下に保つ予算テスト
