# v0.11.0の検証状況

2026-09-08時点。利用者の判断によりworkflowを必須機能として採用し、版番号をv0.11.0 “Sadalmelik”へ揃えた。タグ・公開は別途行う。以下の制御試験を、実モデルの品質・費用比較と区別する。

## 実装と検証の対象

- 段階stateと、全段階共通・段階別の必須skill。分類器とは別に解決し、ターン内で本文と出所hashを固定する。
- sessions-v2の原文、世代比較、削除marker、再開と分岐。中断されたtool呼び出しを検出し、結果不明の処理を自動で再実行しない。
- strict10の直近10実ユーザーターン窓、型付き要約、固定ローカル埋め込み、最大3件・768参照トークンの取得。原文はconversation URIからページ単位で再取得する。
- 通常応答、要約、子実行、埋め込みを区別するattempt台帳。永続台帳APIでは送信前の予約と、失敗・中断・欠測の保持を扱う。
- hosted Webの本文構築、引用・出典の保存と表示、利用できないendpointを要求前に拒否する経路。

workflowは標準採用し、strict10は任意設定を維持する。Webの実接続も未検証で、現行CLIはcached・liveの有効化を拒否する。機能を追加したことを、既定設定の節約効果や実接続の成功として扱わない。実際の設定と制限は[利用ガイド](usage.md)と[コンテキスト効率化](context-efficiency.md)に記載する。

企画・要件定義・実装計画など8種類の追加ケース、採用・版確定の判断、v1.0.0の中規模DBシステム構築による最終試験は[実装前段階の評価計画](preimplementation-evaluation.md)に記載する。以下の完了件数には、この追加試験を含めない。

追加試験は36試行が完了し、2試行はタイムアウトまたは通信エラーによる使用量欠測、10試行は未着手。両条件がそろった17組では、workflow有効時に総tokensが12.7%、API換算費用が12.2%増加した。完了試行は内容評価に合格したが、当初の費用条件は満たしていなかった。2026-09-08に利用者がworkflowを必須機能として採用する判断へ変更した。詳細と比較範囲は上記の評価文書を参照する。

2026-09-08には、純正Codex CLI 0.153.4との3条件比較も完了した。同じ16組では総tokensが追加機能なし88,037、workflow有効99,530、純正493,518、固定単価のAPI換算費用が順に$2.18941、$2.46658、$4.16582。品質は3条件とも16/16合格だった。純正比の費用削減は47.4%／40.8%だが、workflowなし比の増分を理由とする採用停止は、利用者の判断で解除した。純正は承認された直接CLI例外で測定し、個人指示の混入した1件は除外した。実行方法・時間・除外・評価の限界は[3条件比較](preimplementation-evaluation.md#純正codexを含む3条件比較)を参照する。以下の84試行とは別枠である。

## 隔離環境での制御試験

固定したRustドライバー`sadalmelik_quality`は実際のagentループとSessionを使い、provider・要約・埋め込みには疑似応答を返す。判定器は各ターンの期待値と範囲を照合する。ネットワーク推論は行わない。

| 条件 | ケース数 | 合計ターン数 | 結果 |
| --- | ---: | ---: | --- |
| baselineとstrict10の12ターン | 2 | 24 | 制御試験合格 |
| skill 0・3・30、tool 0・3・6、例示0・1・3の9組をbaselineとworkflowで検証 | 18 | 18 | 制御試験合格 |
| 訂正後の再開・分岐時のphase変更を4モードで36ターンずつ検証 | 8 | 288 | 制御試験合格 |
| 合計 | 28 | 330 | 保存行を再検証済み |

4モードはbaseline、workflow、strict10、workflowとstrict10の併用である。全組合せの網羅ではなく、承認済み行列から選んだ条件を使う。tool数はドライバーのadapterで制限し、実モデルのtool選択能力を検証した結果ではない。疑似応答の正答を、GPT-6が古い決定や訂正を正しく解釈した証拠にはしない。

初回のbaseline 12ターンは保存済み成功行を再利用した。残り27ケースはSQLiteを開く前に出力ディレクトリを作成する修正後のバイナリで実行した。行・fixture・バイナリのhashと修正前の失敗ログを保持する。この試験のバイナリはfiles.md自動更新設定の追加前に固定したもので、その設定は別の回帰試験で確認する。

## 実モデルの予備測定

2026-09-07、gpt-6-astra・mediumを要求し、現行candidateの追加機能なしとworkflowのみで各1要求を実行した。tool・分類対象skill・例示は0で、利用者が渡した値を指定のJSONへ返す同じ課題を使った。workflow条件では共通・実装段階の必須skillを実際に注入した。

| 条件 | input | output | cache read | 合計 | 時間 | 正答 |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| 追加機能なし | 174 | 20 | 0 | 194 | 3.80秒 | 合格 |
| workflowのみ | 341 | 20 | 0 | 361 | 3.47秒 | 合格 |
| 合計 | 515 | 40 | 0 | 555 | — | 2/2 |

物理送信は合計2件で、永続台帳・provider診断・agent集計の使用量が一致した。両条件ともcache writeは0と報告され、仕様策定時の固定API単価で換算すると合計$0.00715。Codex契約の実請求額ではない。要求payloadのモデル名と推論強度を確認したが、応答側のモデル識別情報は検証していない。

この予備測定だけは、利用者の承認により厳密な出力・金額保証を外し、最大2物理要求、各120秒、全体240秒と、使用量取得後の超過・欠測停止を適用した。ツール実行、Web、埋め込み、子実行、認証refreshは行っていない。通信隔離と認証の読取り専用は維持した。実行枠を再利用できない起動記録も保存した。

バイナリSHA-256は`f726be3bd22678bfa0e238a0150f8ba24b0d834176c71f91584fa2ff6d93a8a4`。課題、正答、起動スクリプト、115個のソースファイルを固定し、実行後も照合した。各条件1回の短い接続確認であり、旧版baselineとの比較や、キャッシュ率・長期品質・一般的な削減効果の評価には使わない。本測定84試行の成功件数には算入しない。

## 本測定が未実施の理由

本測定はgpt-6-astra、推論強度mediumを指定しているが、実行前検査で84試行すべてをunsupportedとして停止した。この本測定のモデル要求は0件であり、上記2要求の予備測定と区別する。

ChatGPT経由のCodex endpointで、入力・出力・hosted actionの厳密な上限を実効確認できていない。固定multilingual-e5-smallのモデル実体も未導入であり、取得先への通信は拒否されている。公開Responses APIの説明だけで別endpointの対応を推定せず、予備測定の例外を本測定へ広げていない。この84試行については、通常環境への切替や代替モデルによる比較も行っていない。

実行前検査では、最初の作業コピーに必要なソースhash照合対象が欠けていたため停止した。その後、必要なソースをディレクトリ構造ごと固定し、上記の機能未確認を示すレポートを生成した。コピー不備による失敗と、機能未確認による停止を区別している。

次の実測には、endpointの上限制御の証拠と、固定hashを照合した埋め込みruntime・モデルが必要となる。一般的な品質改善・費用削減・利用枠の節約は、この段階では未確認である。

## v0.11.0確定時の検査

2026-09-08、workflowの既定有効化と版番号更新後に`cargo test --locked --offline --workspace`を実行し、1,053件が成功、失敗0件だった。全targetのClippy、fmt、filemapも成功した。workflow既定設定の回帰試験は変更前に失敗し、変更後に全段階の同梱skill解決と384トークン以内を確認した。明示offの継承、CLIの保存済みphaseの復元、TUIの新規v2保存、比較用の機能なし条件を維持する試験も成功している。

この既定切替に伴う実モデルの再測定は行っていない。掲載した比較値は、既に測定済みのworkflow有効条件を使用する。日英README、変更履歴、全10クレートの版番号を0.11.0へ揃え、バイナリの`--version`も確認した。

## 再現用の検査

workspace全体は1,046件成功した。保存ロックの解除を明示する修正後の結果で、core 247件、TUI 296件、filemap 5件を含む。その後、CLIの分岐で保存済みphaseを復元する修正を行い、CLI全15件と全target Clippyを再検証した。この最後のCLI修正後はworkspace全体を繰り返していない。Pythonは埋め込みhelper 4件、Sadalmelik判定器7件が成功した。

保存ロックとCLIのphase復元は、それぞれ修正前に失敗する回帰試験で確認した。自動files.md更新を止めたときの追加要求0件、手動spawnの維持、設定の継承、TUIの新規・再開・分岐も回帰試験の対象に含む。

予備測定の追加分ではprovider 138単体試験・7統合試験、Rustドライバー5件、Python起動制御5件、filemap 5件が成功した。providerのローカルHTTP試験は通常sandboxで待受拒否となり、試験用の待受を許可して再実行した。ドライバーの初回ループ停止設定もオフライン試験で修正した。実モデル送信前には、ツールbrokerを使わない経路の認証環境チェックで1回停止し、起動側で読取り専用を明示してから実測した。この停止ではモデルへ送信していない。今回の変更後にworkspace全体は再実行していない。

```sh
cargo test --locked --offline --workspace
cargo clippy --locked --offline --workspace --all-targets -- -D warnings
cargo fmt --all -- --check
python3 -m unittest discover -s scripts -p 'test_benchmark_sadalmelik.py'
python3 -m unittest discover -s scripts -p 'test_local_embedding.py'
cargo test --locked --offline -p polaris-cli --example sadalmelik_pilot
python3 -m unittest discover -s scripts -p 'test_run_sadalmelik_pilot.py'
```

実モデルの実行許可や能力証明を、疑似ドライバーの成功で代替してはならない。Codex更新から取り入れた設定と根拠は[Codex更新調査](codex-update-efficiency.md)を参照。
